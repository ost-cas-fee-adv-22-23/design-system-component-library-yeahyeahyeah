import { Meta, Typeset, Story, Canvas } from '@storybook/addon-docs';
import { ImageContainer } from '../../components/ImageContainer.tsx';

<Meta title="Guidelines/Docs" component={ImageContainer} />

export const Template = (args) => <ImageContainer {...args} />;

<Canvas>
  <Story
    name="02 Image Container Profile Picture"
    args={{
      alt: 'This is a profile picture',
      fCallBack: () => console.log('ImageContainer clicked'),
      src: 'https://placebeard.it/800x360',
      type: 'container',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
